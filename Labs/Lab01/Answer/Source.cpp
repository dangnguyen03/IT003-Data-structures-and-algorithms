#include "Header.h"
int main() {
	List ds;
	CreatList(ds);
	int members;
	cout << "Nhap so sinh vien: ";
	cin >> members;
	AddListHead(ds,members);
	// Các ý tùy biến theo yêu cầu bằng các lời gọi hàm
}

